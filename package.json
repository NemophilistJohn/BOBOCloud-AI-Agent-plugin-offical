{
  "name": "@bobocloud/ai-agent-plugin-official",
  "version": "1.3.3",
  "private": true,
  "type": "module",
  "description": "Official local AI Agent plugin for BOBOCLOUD.",
  "scripts": {
    "build": "node scripts/package.mjs --build-only",
    "package": "node scripts/package.mjs",
    "release:check": "node scripts/check-release-tag.mjs",
    "test": "node --test tests/extension.test.mjs tests/package.test.mjs",
    "verify": "node scripts/package.mjs --verify-only"
  },
  "engines": {
    "node": ">=20"
  }
}
